# This is a pointer, not a shared runtime environment.
# Copy each component's .env.example to .env.local in that component:
# dashboard/.env.example
# web-caller/.env.example
# client-call-agent/.env.example
# Required variables and their scope: docs/setup.md
